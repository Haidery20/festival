import { NextResponse } from "next/server"

export async function GET() {
  // Stubbed notifications count API.
  // Replace with real data source when available.
  return NextResponse.json({ count: 0 })
}